# RETIRED (2026-07-11)

This fork (branch `esp-idf`) built the AIOsense fleet until 2026-07-11.
The fleet now builds from the overlay repo:
  ~/Projects/joyfulhouse/homeassistant-dev/aiosense  (github: joyfulhouse/aiosense-fleet)

Keep this tree/GitHub repo (archived, read-only) only as the rollback firmware
source for the pre-migration fleet builds. Safe to delete once the rollback
window closes. See homelab docs/superpowers/specs/2026-07-11-aiosense-rebuild-design.md.
